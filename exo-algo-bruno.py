word= "carambar"
letter= [i for i in word]
size=(len(letter))
last_index = size -1

# for i in word:

print([*word])      # ['c', 'a', 'r', 'a', 'm', 'b', 'a', 'r']

for i in range(last_index, 0):
    